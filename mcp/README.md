# mcp/ — capabilities as MCP servers

Anything that must **execute** (e.g. an eval runner, a grilling-gate enforcer) lives here as
an **MCP server**, so it works across MCP-capable tools — one server, many clients.

_(Populated in the MCP module.)_
